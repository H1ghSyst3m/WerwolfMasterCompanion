import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.werwolf.mastercompanion',
  appName: 'Werwolf Master Companion',
  webDir: 'dist-app',
};

export default config;
